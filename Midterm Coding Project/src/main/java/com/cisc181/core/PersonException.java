package com.cisc181.core;

public class PersonException extends Exception{
	public void person() {
	}
	public person = new Person(getDOB(), getFirstName(), getMiddleName(), getLastName(), getAddress(), getPhone(), getEmail);
	
	
}
